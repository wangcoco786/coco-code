{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/jira/:match*", "destination": "/api/jira" },
    { "source": "/:path((?!api/).*)", "destination": "/index.html" }
  ]
}
